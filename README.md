# Instalation

### Dependencies
This script was made for Pop!_OS, so it may not work anywhere else.

### Steps
- clone the repo
- run the script using `cd dot-files && sudo bash ./install.sh`
- reboot
